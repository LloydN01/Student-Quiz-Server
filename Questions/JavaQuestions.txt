MC$What is the output of the following code snippet in Java?<br>int num1 = 10;<br>int num2 = 5;<br>System.out.println(num1/num2);$2.5/2/5/0.5$2
MC$Which of the following is NOT a valid keyword in Java?$private/protected/unsigned/final$protected
MC$What is the difference between == and equals() method in Java?$both are used to compare two objects in Java./== compares the reference while equals() compares the values of two objects./equals() compares the reference while == compares the values of two objects./There is no difference between == and equals() method.$== compares the reference while equals() compares the values of two objects.
MC$What type of programming language is Java?$Code/English/Object-oriented/Function Oriented$Object-oriented
MC$How would you utilise this class to grab its "number" value?<br>public class object(){<br>    Public Int number = 23;<br>    Public String numbers = "38";<br>}<br>Object g = new Object()$g.get(number)/g.number/g.int(number)/get(g).number$g.number
MC$Which of the following is not a primitive data type in java?$int/float/double/string$string
MC$What is the output of this:<br>static int plusMethod(int x, int y) {<br>  return x + y;<br>}<br>static double plusMethod(double x, double y) {<br>  return x + y;<br>}<br>public static void main(String[] args) {<br>  int myNum1 = plusMethod(8, 5);<br>  double myNum1 = plusMethod(4.3, 6.26);<br>  return myNum2<br>}$13/10.56/compile error/0$compile error
MC$Which of the following is not a valid way to declare and initialize an array in Java?$int[] arr = {1, 2, 3};/int arr[] = {1, 2, 3};/int arr[3] = {1, 2, 3};/All of the above are valid.$int arr[3] = {1, 2, 3};
MC$What is the way to print "hello world" in java?$console.log("hello world");/System.out.println("hello world");/print("hello world")/printf("hello world");$System.out.println("hello world");
MC$How do you find the length of a string variable s?$s.length();/s.length;/s.size();/s.size;$s.length();
MC$What would be the result of printing (1 + 2 + "3" + 4)$1234/334/Error/10$334
SA$Write a method to calculate the sum of two numbers entered by the user.$2,3$public static int myMethod(int num1, int num2) {return num1 + num2;}
SA$Write a method to check if a number entered by the user is even. Return True or False.$7$public static boolean myMethod(int number) {return number % 2 == 0;}
SA$Write a method to input a number, add two to it and square it.$3$public static int myMethod(int number) {int result = number + 2;result = result * result;return result;}